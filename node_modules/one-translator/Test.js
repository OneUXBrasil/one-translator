const { Translator } = require('./lib/index.js')
const translator = new Translator()

translator.translate('pt-br', 'en', 'Esta função não está pronta ainda!', false).then((data) => {
	console.log(data)
})


